apiVersion: v1
kind: Service
metadata:
  name: web-service-tf09
spec:
  type: NodePort
  selector:
    app: web-tf09
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30000