apiVersion: v1
kind: Service
metadata:
  name: ads-site-service
  namespace: ads-unifaat
  labels:
    app: ads-site
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: ads-site
